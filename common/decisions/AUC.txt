AUC_war_category = {
    AUC_war_mission = {
		priority = 10
		icon = GFX_decision_revolt
		allowed = {
			tag = AUC
		}
        fire_only_once = yes
		available = {
			hidden_trigger = { always = no }
		}
		activation = {
			987 = { is_fully_controlled_by = AUC  } 
		}
	    days_mission_timeout = 22
		priority = 10
		timeout_effect = {
            declare_war_on = {
                target = VEP
                type = annex_everything
            }
			load_oob=AUC_Chelyaba
			add_timed_idea = {
				idea=AUC_operation_chelyaba
				days = 90
			}
			990 = {
				transfer_state_to = AUC
			}
	    }
    }
	    AUC_Take_Magnitogorsk = {
		priority = 10
		icon = GFX_decision_revolt
		allowed = {
			tag = AUC
		}
        fire_only_once = yes
		available = {
			hidden_trigger = { always = no }
		}
		activation = {
			has_war_with=VEP 
		}
	    days_mission_timeout = 15
		timeout_effect = {
            add_power_balance_value = -0.2
	    }
    }
}